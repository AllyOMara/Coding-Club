# Defining the number from the user input, converting into an integer
number = int(input('Number? '))

# Turning the integer into the character it represents in Unicode
letter = chr(number)

# Prints the Unicode character
print(letter)
